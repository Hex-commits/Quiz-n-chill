# Secrets
.env
.env.local
.env.*.local

# Node
node_modules/
.next/
out/
next-env.d.ts

# Python
__pycache__/
*.py[cod]
# Regenerated by Vercel's builder on every deploy. Committing it adds a second
# thing to keep in step with pyproject.toml, and a stale lock fails the build
# rather than quietly resolving.
uv.lock
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Supabase CLI local state
supabase/.branches/
supabase/.temp/
supabase/.env

# Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Ingest run reports
tools/ingest/out/
